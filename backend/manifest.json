{
  "id": "knowledge",
  "name": "Knowledge / RAG",
  "version": "0.1.0",
  "coreVersion": "^0.1.0",
  "dependencies": ["postgres", "ai", "minio", "redis"],
  "provides": ["knowledge.Retriever"],
  "consumes": ["postgres.Connection", "ai.LLM", "minio.Storage", "redis.Queue"],
  "permissions": ["knowledge.view", "knowledge.ingest"],
  "routes": ["/api/knowledge"],
  "events": { "emits": ["knowledge.ingested"], "listens": [] },
  "config": { "schema": "./config.schema.json" },
  "migrations": "plugin-owned (backend/migrate.py: CREATE EXTENSION vector -> create_all -> HNSW; run by scripts.migrate_plugins)"
}
